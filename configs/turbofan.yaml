data:
  dataset: turbofan
  window: 30
  stride: 1
  batch_size: 256
  num_workers: 2

model:
  hidden: 64
  rnn_layers: 2
  attn_layers: 2
  heads: 4
  dropout: 0.1

train:
  epochs: 50
  lr: 1.0e-3
  weight_decay: 1.0e-5
  grad_clip: 1.0
  patience: 8

loss:
  alpha: 0.35
  gamma: 2.0
